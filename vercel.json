{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm verify:frontend",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!robots.txt|favicon.ico|favicon.svg|assets/.*).*)",
      "destination": "/index.html"
    }
  ]
}
